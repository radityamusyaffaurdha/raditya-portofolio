# Images needed here

Add these files (same names the code already references):

- `profile.jpg` — hero ID card photo (square-ish, at least 300x300px)
- `ugm/ugm1.jpg`, `ugm/ugm2.jpg`, `ugm/ugm3.jpg` — used as a blurred
  background collage behind the Dream University section. Any of the
  three can be missing — that slot automatically falls back to an
  elegant gradient placeholder instead of a broken image.
- `certificates/` — optional, drop certificate images here later and
  reference them via the `image` field in `data/certificates.ts`
